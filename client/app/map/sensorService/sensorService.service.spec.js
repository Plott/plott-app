'use strict';

describe('Service: sensorService', function () {

  // load the service's module
  beforeEach(module('plottAppApp'));

  // instantiate service
  var sensorService;
  beforeEach(inject(function (_sensorService_) {
    sensorService = _sensorService_;
  }));

  it('should do something', function () {
  });

});
